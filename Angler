if(first()) {
    runOnTick(1)
    
    holoCreate(1)
    holoPos(1,entity():toWorld(vec(0,0,50)))
    holoScale(1,vec(10,1,1))
}

Heading = heading(entity():pos(),ang(0,0,180),owner():pos())
holoAng(1,Heading)
